/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // 移除 output: 'export' 以启用完整的 Next.js 功能
  // 移除 images: { unoptimized: true } 以启用图片优化
};

export default nextConfig;
